export interface IPerson {
    id: number,
    firstName: string,
    lastName: string,
    email: string,
    productId: number,
    //productId está sendo implementado apenas por motivo didático, ele não estará aqui na versão final
}